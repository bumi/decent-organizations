.cell
  %h4 Add your comment
.cell
  = form_for(Comment.new, url: post_comments_path(post.id)) do |f|
    = f.label :name, 'Name'
    = f.text_field :name
    = f.label :text, 'Text'
    = f.text_area :text
    = f.submit 'Save', class: 'button'
